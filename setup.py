#!/usr/bin/env python

from setuptools import setup, find_packages
distribution_name = 'feed'

setup(
    name='feed',
    version='0.1',
    author='jhylands',
    author_email='hylands.james@gmail.com',
    packages=find_packages(),
    install_requires=[],
    package_data={},
    include_package_data=False,
    entry_points={},
)

