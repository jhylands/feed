<html>
<head>
</head>
<body>
    <table>
{% for item in items %}
    <tr>
    <td>
    <img src="{{item.image}}" />
    </td>
    <td><a href="{{item.link}}">{{item.title}}</a></td>
    </tr>

{% endfor %}
    </table>
</body>
</html>
